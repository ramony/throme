version: '3'
services:
  throme:
    build: .
    ports:
      - "8000:3000"
    environment:
      # 在这里可以添加你的环境变量
      # 例如：
      # - NODE_ENV=production
      # - API_URL=http://your-api-url
      NODE_ENV: production
      DB_HOST: mysql
    # volumes:
    #   - .:/app
    restart: always